<Window
  x:Class="Banking.Views.OverviewWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:local="clr-namespace:Banking.Views"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  Title="Overview"
  Width="800"
  Height="470"
  ShowInTaskbar="False"
  mc:Ignorable="d">
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="5" />
      <ColumnDefinition Width="*" />
      <ColumnDefinition Width="5" />
    </Grid.ColumnDefinitions>
    <Grid.RowDefinitions>
      <RowDefinition Height="5" />
      <RowDefinition Height="Auto" />
      <RowDefinition Height="*" />
      <RowDefinition Height="Auto" />
    </Grid.RowDefinitions>

    <StackPanel
      Grid.Row="1"
      Grid.Column="1"
      Orientation="Horizontal">
      <TextBlock VerticalAlignment="Center" Text="Overzicht: " />
      <ComboBox
        Name="OverviewsComboBox"
        Margin="0,0,0,2"
        SelectionChanged="OverviewsComboBox_SelectionChanged" />
      <Button
        Name="ExportButton"
        Margin="5,0,0,2"
        HorizontalAlignment="Right"
        Click="ExportButton_Click"
        Content="_Copy export" />
    </StackPanel>

    <DataGrid
      Name="PivotDataGrid"
      Grid.Row="2"
      Grid.Column="1"
      AlternatingRowBackground="#FFEEEEEE"
      AutoGenerateColumns="True"
      AutoGeneratedColumns="PivotDataGrid_AutoGeneratedColumns"
      CanUserAddRows="False"
      CanUserDeleteRows="False"
      FrozenColumnCount="1"
      IsReadOnly="True"
      MouseDoubleClick="PivotDataGrid_MouseDoubleClick">
      <DataGrid.ColumnHeaderStyle>
        <Style TargetType="{x:Type DataGridColumnHeader}">
          <Setter Property="LayoutTransform">
            <Setter.Value>
              <RotateTransform Angle="-90" />
            </Setter.Value>
          </Setter>
        </Style>
      </DataGrid.ColumnHeaderStyle>
      <DataGrid.CellStyle>
        <Style TargetType="{x:Type DataGridCell}">
          <Setter Property="HorizontalAlignment" Value="Right" />
        </Style>
      </DataGrid.CellStyle>
    </DataGrid>
  </Grid>
</Window>
